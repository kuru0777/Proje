import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        padding: 0, // Sıfır olarak ayarlandı
        borderRadius: 5,
        backgroundColor: "#C08261",
        marginLeft:5,
        marginRight:5,
        marginTop:5
    },
    searchTitle: {
        color: "white",
        borderWidth: 1,
        borderColor: "grey"
    }
});
